{
  "name": "@meridian/billing-engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@meridian/billing-core": "workspace:*",
    "@meridian/db": "workspace:*",
    "@meridian/otel": "workspace:*",
    "@meridian/shared-types": "workspace:*",
    "pino": "^9.3.0"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}
